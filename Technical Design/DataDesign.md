# Data Design

- Database schema
- Tables / collections
- Relationships
- Indexes (if important)

Shown using ER diagrams or UML class diagrams

Now define:
Song data format (JSON? ScriptableObject?)
Note structure
Save file structure
Player progress model

Architecture comes before data because data supports the architecture.